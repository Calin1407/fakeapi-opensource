{
  "name": "fake-api",
  "description": "This is a Fake Api to TerraTech Project",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT --routes routes.json --cors db.json",
    "dev": "json-server --watch db.json --routes routes.json --port 3000"
  },
  "dependencies": {
    "json-server": "0.17.4"
  }
}
