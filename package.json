{
  "name": "fake-api",
  "description": "This is a Fake Api to Open Source",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT --routes routes.json db.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
